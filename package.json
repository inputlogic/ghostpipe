{
  "name": "ghostpipe",
  "version": "0.0.1",
  "description": "Ghostpipe is a CLI tool that connects codebase files to custom web-based GUIs",
  "main": "index.js",
  "bin": {
    "ghostpipe": "./bin/ghostpipe.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cli"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
